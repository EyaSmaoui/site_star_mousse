{
  "name": "site_star_mousse",
  "version": "1.0.0",
  "main": "backend/app.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node backend/app.js",
    "dev": "nodemon backend/app.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && node scripts/copyBuild.js",
    "dev:all": "concurrently \"npm run dev\" \"npm run start --prefix frontend\""
  },
  "dependencies": {
    "axios": "^1.16.1",
    "chart.js": "^4.5.1",
    "playwright": "^1.40.0",
    "react-chartjs-2": "^5.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^2.0.22"
  }
}
